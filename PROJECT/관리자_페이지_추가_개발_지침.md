# 관리자 페이지 추가 개발 지침

> 기존 `ADMIN_PORTAL_GUIDE.md`에 더해, **추가로 반영된** 관리자 관련 기능·API·설정을 정리한 문서입니다.  
> 관리자 페이지 **추가 개발 시** 이 지침을 기준으로 연동하면 됩니다.
>
> 화면 구성(레이아웃/필드/액션/운영 플로우)은 **`관리자_페이지_화면설계안.md`** 참고.  
> **담당자 검수**(입력폼·이미지·인식정보 확인·검수 절차) 및 **관리자 페이지–BE 연동 API**는 **`관리자_검수_기능_지침_및_API_연동.md`** 참고.

---

## 1. 판정 규칙 — 유효일 설정 (MinIO–DB 정합)

### 1.1 추가된 필드

| 필드 | 타입 | 기본값 | 범위 | 설명 |
|------|------|--------|------|------|
| `orphan_object_days` | int | 1 | 1~365 | **고아 객체 유효일(일)**. MinIO에만 있고 DB에 submission이 없는 객체를 이 일수 경과 후 삭제 후보로 볼 때 사용. |
| `expired_candidate_days` | int | 1 | 1~365 | **만료 후보 유효일(일)**. DB에 submission은 있으나 receipt_items가 없는(Complete 미호출) 건을 만료 후보로 볼 때 사용. |

### 1.2 API

- **조회**: `GET /api/v1/admin/rules/judgment`  
  - 응답에 `orphan_object_days`, `expired_candidate_days` 포함 (없으면 1).
- **수정**: `PUT /api/v1/admin/rules/judgment`  
  - body에 `orphan_object_days`, `expired_candidate_days` 포함 가능. 1~365만 유효.

### 1.3 관리자 화면 반영

- 판정 규칙 설정 화면에 **고아 객체 유효일(일)**, **만료 후보 유효일(일)** 입력 필드 추가.
- 기본값 1일, 1~365 범위 제한.
- 저장 시 기존 판정 규칙과 함께 `PUT /api/v1/admin/rules/judgment`로 전송.

### 1.4 DB

- `judgment_rule_config` 테이블에 컬럼 추가 필요.  
  - 마이그레이션: `PROJECT/migrations/judgment_rule_config_minio_days.sql`

---

## 1-2. 신규 상점 정책 — 자동 상점추가 기본값 (데이터 자산화)

### 1-2.1 기본값

- **`unknown_store_policy`** 기본값: **AUTO_REGISTER** (신규상점검수대기 → 자동 상점추가).
- 마스터에 없는 상점이어도, 분류 신뢰도가 임계값 이상이면 **자동으로** `master_stores` + `unregistered_stores`(AUTO_REGISTERED)에 등록하고 해당 영수증은 **FIT** 처리.
- 데이터 자산화·관리: 등록된 상점은 후보군/마스터에서 조회·관리 가능.

### 1-2.2 값

| 값 | 설명 |
|----|------|
| **AUTO_REGISTER** (기본) | 자동 상점추가 후 FIT. 데이터 자산화. |
| PENDING_NEW | 신규 상점 검수 대기(관리자 승인 후 편입). |

### 1-2.3 DB/마이그레이션

- 기존 행이 `PENDING_NEW` 또는 NULL 이면 `AUTO_REGISTER` 로 통일:  
  `PROJECT/migrations/judgment_rule_default_auto_register.sql`

---

## 2. 신청(submission) 상세 — FE 제출 데이터 스냅샷

### 2.1 추가된 응답 필드

- **API**: `GET /api/v1/admin/submissions/{receiptId}`  
  - 응답에 **`user_input_snapshot`** 포함 (있을 경우).
  - Complete 요청 시 FE가 보낸 `data`(방식2: `data.items[]`)를 그대로 저장한 JSON.  
  - 관리자 화면에서 **FE 입력 vs OCR 결과** 나란히 비교할 때 사용.

### 2.2 관리자 화면 반영

- 신청 상세 화면에서 **FE 제출 데이터(user_input_snapshot)** 를 노출.
  - 예: 장별 사용자 입력 금액·결제일·상호 등과 OCR 결과·이미지를 나란히 표시.

---

## 3. 이미지 presigned URL — Content-Type 지정

### 3.1 변경 사항

- **API**: `GET /api/v1/admin/receipts/{receiptId}/images`  
  - 응답의 `items[].image_url`은 MinIO presigned GET URL.
  - BE에서 **확장자에 따라** `ResponseContentType` 지정 (`.png` → `image/png`, `.jpg`/`.jpeg` → `image/jpeg`).
  - 저장 시 Content-Type이 잘못돼 있어도 브라우저에서 이미지로 표시되도록 함.

### 3.2 관리자 화면

- 별도 변경 없음. 기존처럼 `image_url`로 이미지 표시하면 됨.

---

## 4. 수동 판정(Override) · 콜백 재전송

- 기존과 동일.  
  - **Override**: `POST /api/v1/admin/submissions/{receiptId}/override` (status, reason, override_reward_amount, resend_callback).  
  - **콜백 재전송**: `POST /api/v1/admin/submissions/{receiptId}/callback/resend` (optional target_url).  
- **콜백 검증(신규, 관리자 클릭 시 즉시 송출)**: `POST /api/v1/admin/submissions/{receiptId}/callback/verify`  
  - BE가 `OCR_RESULT_CALLBACK_URL`로 실제 POST를 보내고, 응답(status/ok/elapsed_ms)을 JSON으로 반환.
- **콜백 전송 로그 조회(신규)**: `GET /api/v1/admin/submissions/{receiptId}/callback/logs` (limit=1~200)  
  - `CALLBACK_SEND`(자동/검증/재전송 실제 송출 결과: status/ok/elapsed_ms/에러), `CALLBACK_VERIFY`(검증 버튼 클릭 기록), `CALLBACK_RESEND`(재전송 트리거 기록) 확인용.
- 상세는 `ADMIN_PORTAL_GUIDE.md` 참고.

---

## 5. 멈춘 신청(PROCESSING/VERIFYING) 복구

### 5.1 개요

- OCR 분석이 오래 걸리거나 중단되어 **PROCESSING/VERIFYING** 상태에 머무는 건이 있을 수 있음.
- 관리자 페이지에서 **직접 상태를 바꾸는 API는 없음**.  
  - 운영·개발 환경에서 **스크립트** 또는 **SQL**로 복구.

### 5.2 관리자 페이지에서 할 수 있는 것

- **신청 목록/상세**에서 status가 PROCESSING/VERIFYING인 건을 **필터·표시**.
- 안내 문구 예: “일정 시간 이상 처리 중이면 운영자에게 문의. 복구는 백엔드 스크립트로 진행.”

### 5.3 복구 방법 (참고)

- **스크립트**: `PROJECT/scripts/mark_stuck_submissions_error.py` (특정 receiptId 또는 `--all --minutes N`).
- **SQL**: `PROJECT/scripts/mark_stuck_submissions_error.sql` (주석 해제 후 실행).  
- 자세한 사용법: `PROJECT/scripts/README.md`.

---

## 6. MinIO–DB 정합성 (리콘실리레이션)

### 6.1 개요

- MinIO에는 이미지가 있는데 DB에는 해당 submission이 없거나, Complete가 호출되지 않은 경우가 있음.
- **유효일**은 위 **1. 판정 규칙**의 `orphan_object_days`, `expired_candidate_days`로 관리자가 설정.

### 6.2 관리자 페이지에서 할 수 있는 것

- 유효일 설정은 **판정 규칙 화면**에서만 하면 됨 (1번 참고).
- **리콘실 실행**은 백엔드/배치에서 스크립트로 수행 (`PROJECT/scripts/reconcile_minio_db.py`).  
  - 관리자 페이지에서 “리콘실 실행” 버튼을 둘 경우, 별도 **관리자 전용 API**가 필요하면 BE에 요청.

### 6.3 정책

- 고아 객체: MinIO에만 있음 → 객체 **LastModified** 기준 `orphan_object_days` 일 경과 후 삭제 후보.
- 만료 후보: DB에 submission만 있고 receipt_items 없음 → **created_at** 기준 `expired_candidate_days` 일 경과 후 만료 후보로 보고.  
- 자세한 정책: `MinIO-DB_정합성_및_OCR미인식_정책.md`.

---

## 7. 체크리스트 — 관리자 페이지 추가 개발 시

- [ ] **판정 규칙**: `orphan_object_days`, `expired_candidate_days` 조회·수정 (기본 1일, 1~365).
- [ ] **신청 상세**: `user_input_snapshot` 노출 (FE 입력 vs OCR 비교 가능하도록).
- [ ] **이미지 표시**: 기존 이미지 URL 사용 (BE에서 이미 Content-Type 지정 적용됨).
- [ ] **Override / 콜백**:
  - [ ] 검수 완료(override) UI에서 `resend_callback` 기본 ON 권장(검수 완료 시 자동 콜백 송출).
  - [ ] 콜백 검증 버튼(`POST .../callback/verify`) 및 콜백 로그(`GET .../callback/logs`) 노출 권장.
- [ ] **멈춘 신청**: PROCESSING/VERIFYING 필터·표시 및 “복구는 스크립트로 진행” 안내.
- [ ] **마이그레이션**: `judgment_rule_config_minio_days.sql` 적용 여부 확인.

---

## 8. 카드번호(card_num) 코드 구분

| 값 | 의미 | DB 저장 |
|------|------|--------|
| **0000** | 현금 결제 | 현금일 때만 |
| **1000** | 카드번호 없음 / **** 마스킹 / 미표시 | 카드 정보 없을 때 |
| **4자리 숫자** | 카드번호 마지막 4자리 | OCR에서 인식된 경우 |

- Status·상세 API의 `items[].extracted_data.card_num`(또는 `card_num`)에 위 값이 내려옵니다.
- 관리자·FE에서 노출 시: `0000` → "현금", `1000` → "카드번호 없음", 그 외 → "****-****-****-1234" 등으로 표시하면 됩니다.

---

## 9. OCR_004 (인식 불량·수동 검수 보정)

- **error_code = OCR_004**: 상점명·사업자번호·주소 등 핵심 필드가 비어 있거나, OCR 컨피던스가 낮을 때 자동 부여.
- 해당 장은 **PENDING_VERIFICATION** 상태로, 관리자 페이지에서 **상세 조회 후 보정**할 수 있도록 노출 권장.
- 보정 후 기존처럼 override API로 FIT/UNFIT 등 최종 판정 처리.
- 환경 변수: `OCR_LOW_CONFIDENCE_REVIEW_THRESHOLD`(기본 70), `OCR_KEY_FIELDS_MIN_FILLED`(기본 2). 자세한 조건은 `네이버_CLOVA_OCR_레퍼런스_및_인식률_검토.md` §6 참고.

---

## 10. 참조 문서

| 문서 | 내용 |
|------|------|
| `ADMIN_PORTAL_GUIDE.md` | 관리자 웹 연동 전반, 판정 규칙·후보군·override·콜백 등. |
| `FE_API_규격_문서.md` | 관리자 API 상세 (5·6·7번). |
| `MinIO-DB_정합성_및_OCR미인식_정책.md` | 고아/만료 후보 정책, OCR 미인식 검증. |
| `scripts/README.md` | 멈춘 신청 복구, 리콘실, S3 이미지 진단 스크립트. |
| `관리자_페이지_화면설계안.md` | 관리자 화면 레이아웃/필드/액션/운영 플로우(BE 기준). |

---

## 11. 관리자 페이지 구성을 위한 BE 적용 사항 — 행정구역 · 통계

관리 주체가 **지자체**일 때, 관리자 페이지에서 **행정구역 풀다운(시도 → 시군구)** 과 **행정구역별 통계**를 제공하기 위한 BE 보완 사항입니다.

### 11.1 관리자 페이지에서 필요한 API (행정구역·통계)

| 용도 | Method | Path / Query |
|------|--------|----------------|
| 시도 목록(풀다운 1단계) | GET | `/api/v1/admin/regions/sido` |
| 시군구 목록(풀다운 2단계) | GET | `/api/v1/admin/regions/sigungu?sido={code}` (또는 시도 이름) |
| 행정구역별 통계 | GET | `/api/v1/admin/stats/by-region?sido=...&from=...&to=...` (선택: `sigungu=...`) |
| **행정지도 SVG URL** | GET | `/api/v1/admin/maps/svg/url?level=sido` 또는 `?level=sigungu&sido={code}` |

- **시도/시군구**: 데이터 소스 `PROJECT/data/regions_kr.json`. **전 시도(17개)에 대해 시군구 목록이 정의되어 있음.** `sido`는 코드(예: `42`) 또는 이름(예: `강원특별자치도`) 모두 가능.
- **통계**  
  - `from`, `to`: 기간(YYYY-MM-DD 등). `dateFrom`, `dateTo` 와 동일하게 동작.  
  - 파라미터 없음 → 시도별 집계 / `sido`만 → 해당 시도 시군구별 / `sigungu` 추가 → 해당 시군구 단일 집계.  
  - 집계 기준: submission 당 **첫 장(seq_no=1)** 의 `address`/`location`.

### 11.2 행정지도 SVG 표시 (statgarten/maps, SGIS)

관리자 페이지에 **행정경계 지도**를 넣으려면 [statgarten/maps](https://github.com/statgarten/maps)(통계청 SGIS API 기반) SVG를 사용합니다. BE에서 **표시할 SVG의 URL**만 제공하고, 실제 파일은 GitHub raw 또는 FE에서 해당 URL로 로드합니다.

#### 11.2.1 API

- **GET** `/api/v1/admin/maps/svg/url`
  - **Query**: `level` = `sido`(전국 시도 경계) | `sigungu`(시군구 경계). `level=sigungu` 일 때 `sido` = 시도 코드(예: `42`) 또는 이름(예: `강원`) 필수. **`variant=simple`** (선택): 단순화 SVG 사용(용량 작음, path id 구조 동일).
  - **Response**: `url`, `source`, `level`, `sidoCode`, `sidoName`, `variantUsed`, **`pathIdHint`**(행정구역별 구분 표시 안내).
  - `url`: 관리자 페이지에서 **인라인 SVG 로드용**(권장) 또는 img src 로 사용.

#### 11.2.2 행정구역별로 구분되어 보이게 하기

[statgarten/maps](https://github.com/statgarten/maps) SVG는 **각 행정구역이 `<path id="시도명 또는 시군구명">`** 형태로 들어 있습니다. 예: 전국 시도 지도는 `id="서울특별시"`, `id="강원도"` 등, 시군구 지도는 `id="춘천시"`, `id="원주시"` 등.

- **`<img src={url}>`로 넣으면** SVG 내부를 스타일할 수 없어 **구역이 구분되지 않습니다.**
- **행정구역별로 구분해서 보이려면** 다음처럼 **SVG를 인라인으로 삽입**한 뒤, `path`에 CSS를 적용해야 합니다.

1. **URL로 SVG 내용 가져오기**: `fetch(response.url)` → `res.text()` 로 SVG 문자열 획득.
2. **DOM에 인라인 삽입**: 해당 문자열을 컨테이너의 `innerHTML` 또는 React라면 `dangerouslySetInnerHTML` 등으로 넣기. (또는 `<object data={url}>` 사용 시 같은 출처가 아니면 내부 스타일 제한이 있을 수 있음.)
3. **CSS로 구역 구분**  
   - 기본: `path { fill: #e8e8e8; stroke: #fff; stroke-width: 0.5; }`  
   - 호버: `path:hover { fill: #bbdefb; }`  
   - 특정 구역만 강조: `path#강원도 { fill: #c8e6c9; }` (시도 지도), `path#춘천시 { fill: #c8e6c9; }` (시군구 지도)
4. **통계 연동**: `GET .../stats/by-region` 으로 구역별 수치를 가져와, 해당 구역 `path#id`에 툴팁/뱃지로 표시하면 됩니다.

**권장**: `?variant=simple` 로 요청하면 `svg/simple/` 단순화 SVG를 쓰며, 파일 크기가 작고 **path id 구조는 동일**하므로 위 스타일 방식을 그대로 사용할 수 있습니다.

#### 11.2.3 관리자 페이지에서 표시 방법 요약

1. **전국 시도 지도**: `GET .../maps/svg/url?level=sido` 또는 `?level=sido&variant=simple` → 응답 `url` 사용.
2. **특정 시도 시군구 지도**: `GET .../maps/svg/url?level=sigungu&sido=42` (필요 시 `&variant=simple`).
3. **표시 예시**
   - **인라인(권장, 구역 구분 가능)**: `fetch(url)` → SVG 텍스트 → 컨테이너에 삽입 후 위 CSS 적용.
   - **img(구역 구분 불가)**: `<img src={url} alt="행정지도" />` — 경계선만 보이고 구역별 색/호버 불가.

#### 11.2.4 데이터 출처

- **statgarten/maps**: [https://github.com/statgarten/maps](https://github.com/statgarten/maps) — SGIS API(통계청) 기반 대한민국 행정경계 GeoJSON/SVG.
- BE는 `PROJECT/data/regions_kr.json` 내 `statgarten_svg` 매핑으로 시도 코드 → SVG 파일명을 참조하며, 실제 URL은 `https://raw.githubusercontent.com/statgarten/maps/main/svg/{파일명}` 입니다.
