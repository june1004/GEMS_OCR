# GEMS Admin 백엔드(BE) 요청 사항 정리

관리자 페이지(프론트엔드)에서 필요로 하는 API 및 데이터 요청 사항을 정리한 문서입니다.

---

## 1. 로그인 API – 조직 정보 포함

**상세:** [로그인_API_조직정보_요청사항.md](./로그인_API_조직정보_요청사항.md)

- **POST /api/v1/auth/login** 응답의 `user` 객체에 아래 필드 포함 요청.
  - `organization_id` / `organizationId`: 조직 ID (미반환 시 프론트 "api (미반환)" 표시)
  - `org_name` / `orgName`: (선택) 조직명
  - `org_type` / `orgType`: (선택) 조직 유형
  - `campaignIds`: (선택) 해당 사용자가 접근 가능한 캠페인 ID 배열 → 기관/검수자 권한 필터에 사용

---

## 2. 제출(Submissions) API – 대시보드·검수용

현재 **GET /api/v1/admin/submissions** 를 전제로 한 추가/확인 사항입니다.

### 2.0 422 Unprocessable Content 대응 (우선 요청)

아래와 같이 호출 시 **422**가 발생하고 있습니다.  
→ **원인**: 쿼리 파라미터(`status`, `from`, `to`, `campaignId`)를 BE에서 아직 허용하지 않거나, 파라미터 이름이 다를 가능성.

| FE가 보내는 예시 | 용도 |
|------------------|------|
| `?status=APPROVED&campaignId=1&limit=1000&offset=0` | 승인 건 금액 합계(예산 소진률) |
| `?from=2026-03-04&to=2026-03-10&campaignId=1&limit=500` | 최근 7일 접수 추이·유형별 비중 |

**BE 측 요청 사항**

- **옵션 A**: 위 파라미터 이름 그대로 지원  
  - `status`, `from`, `to`, `campaignId` (camelCase), `limit`, `offset` 수용
- **옵션 B**: snake_case만 지원하는 경우  
  - 지원하는 파라미터 이름을 알려주시면 FE에서 맞춰서 요청하도록 수정 가능  
  - 예: `campaign_id`, `date_from`, `date_to` 등

422가 나오는 동안은 FE에서 해당 요청을 빈 데이터(`total: 0`, `items: []`)로 처리해 두었으며, BE 반영 후에는 자동으로 실제 데이터가 표시됩니다.

---

### 2.1 쿼리 파라미터 지원

| 파라미터 | 용도 | 비고 |
|----------|------|------|
| `from` | 시작일 (YYYY-MM-DD) | 금일/어제/최근 7일 집계에 사용 |
| `to` | 종료일 (YYYY-MM-DD) | |
| `campaignId` | 캠페인 ID로 필터 | 기관·검수자: 자신의 캠페인만 조회 |
| `status` | MANUAL_REVIEW, APPROVED 등 | 검수 대기 건수, 승인 건 집계에 사용 |
| `limit`, `offset` | 페이징 | |

- **응답**: `{ total: number, items: AdminSubmissionSummary[] }` 형태 유지 요청.
- **items[]** 각 항목에 **project_type** (또는 projectType) 필드 있으면 좋음: `STAY`(숙박) / `TOUR`(일반 소비) 등. 대시보드·서브미션 업종 필터에 사용.

**제출 항목 확장 필드 (선택, FE에서 미제공 시 비활성 표시)**

| 필드 | 용도 |
|------|------|
| `thumbnail_url` | 서브미션 목록·호버 시 썸네일 미리보기 |
| `confidence` | 신뢰도 슬라이더 필터 (0~100, 낮은 건만 검수 우선) |
| `integrityCheck` (boolean) | 무결성 검사 결과, 목록에 OK 표시 |

### 2.2 (선택) 대시보드 전용 집계 API

한 번의 호출로 대시보드 수치를 내려받고 싶을 경우 아래 형태의 API를 검토해 주시면 좋습니다.

- **GET /api/v1/admin/dashboard/stats** (또는 유사 경로)
- **쿼리**: `campaignId`, `from`, `to` (예: 오늘, 어제, 최근 7일)
- **응답 예시**:
  - `todayCount`, `yesterdayCount`: 금일/전일 제출 건수 (전일 대비 증감률 계산용)
  - `pendingCount`: MANUAL_REVIEW 건수
  - `approvedAmountSum`: 승인(APPROVED) 건의 total_amount 합계 (예산 소진률용)
  - `byCategory`: `{ STAY: number, TOUR: number }` 등 (유형별 건수)
  - `dailyCounts`: `{ date: string, count: number }[]` (일자별 추이 차트용)

미구현 시에도 현재처럼 **GET /api/v1/admin/submissions** 여러 번 호출로 프론트에서 집계 가능하나, 성능·일관성 측면에서 집계 API가 있으면 유리합니다.

---

## 3. 일괄 반려 API (Bulk Rejection)

서브미션(제출 검색) 페이지에서 **일괄 반려 사유 입력 팝업**으로 선택 건에 대해 반려 처리·알림·학습 데이터 태깅을 할 수 있도록, 아래 API 및 연동을 요청합니다.

### 3.1 일괄 반려 실행 API

- **POST /api/v1/admin/submissions/bulk-reject** (또는 유사 경로)
- **Request Body 예시**:
  ```json
  {
    "receiptIds": ["uuid-1", "uuid-2", ...],
    "reasonCode": "image_unreadable",
    "reasonMessage": "제출하신 영수증의 글자가 흐려 금액 확인이 어렵습니다. 재촬영 후 제출 바랍니다.",
    "tagAsError": true
  }
  ```
  - `reasonCode`: 프리셋 ID (예: image_unreadable, duplicate, out_of_scope, below_min_amount)
  - `reasonMessage`: 시민에게 전달할 반려 사유 문구 (알림톡 등에 사용)
  - `tagAsError`: true 시 Sidecar JSON 등에 `is_error: true` 학습 데이터 태그

- **기대 동작**:
  - 각 submission의 `status`를 `UNFIT` 또는 `REJECTED`로 변경
  - (선택) 각 userUuid에 매핑된 연락처로 반려 사유 포함 카카오 알림톡/푸시 발송
  - (선택) 보안 스토리지 Sidecar JSON에 `rejection_reason`, 검수자 ID, 일시 기록
  - `tagAsError: true`인 경우 해당 건을 부정수급/오류 학습 데이터셋으로 분류

### 3.2 감사(Audit) 로그

- 일괄 반려 실행 시 **누가, 언제, 몇 건, 어떤 사유로** 실행했는지 Audit Log에 기록 요청 (IP 포함 시 지자체 보안 감사 대비).

---

## 4. 반려 사유 집계 (선택)

대시보드 “주요 반려 사유 Top 3” 표시를 위해, 반려 사유별 건수를 내려주는 API가 있으면 좋습니다.

- 예: **GET /api/v1/admin/receipts/reject-reasons** (또는 기존 제출/영수증 API 확장)
- **응답 예시**: `[{ reason: "이미지 흐림", count: 32 }, { reason: "금액 불일치", count: 15 }, ...]`
- 미구현 시 프론트에서는 “API 연동 시 표시” 문구만 노출합니다.

---

## 5. 운영 컨텍스트 (Admin Context)

- **GET /api/v1/admin/context**: 현재 관리자의 선택 캠페인(projectId) 등 저장값 조회
- **PUT /api/v1/admin/context**: 선택 캠페인 등 저장  
→ 이미 008_admin_context 마이그레이션 및 라우트 구현된 경우, 유지해 주시면 됩니다.

---

## 6. 캠페인(Campaigns) API

- **GET /api/v1/admin/campaigns**, **GET /api/v1/admin/campaigns/:id**  
  - 응답에 **start_date**, **end_date**, **budget** 포함되어 있으면 대시보드 “캠페인 기간(D-Day)”, “예산 소진 현황”에 사용합니다.
- **campaignId** 기준 제출/집계 API와 연동되면, 기관·검수자는 자신에게 허용된 캠페인만 조회 가능해야 합니다.

---

## 7. 영수증 이미지 (보안 스토리지)

- **GET /api/v1/admin/receipts/:receiptId/images**  
  - 보안 스토리지(Secure Storage) 사용 시, 원본 이미지는 **임시 Signed URL** 등으로 단기간만 노출되도록 구성해 주시면, 지자체 보안 가이드라인 준수에 유리합니다.

---

## 8. 영수증 상태 조회 – 열람 이력·데이터 자산화

영수증 상태 조회 화면에서 **증거 보기(모달)** 사용 시 다음 연동이 있으면 좋습니다.

- **열람 이력 기록**: 증거 이미지/상세를 열람할 때마다 **관리자 ID**, **조회 시간**, **조회 IP**, **receiptId**를 감사 로그에 기록해 주시면, 개인정보 보호법·지자체 보안 지침 대응에 유리합니다.
- **데이터 자산화 태깅 (선택)**: 조회 화면에서 “AI 학습용 우수 사례” / “부정수급 의심 사례”로 분류하는 API가 있으면, 해당 receiptId를 학습 데이터셋 폴더 또는 태그와 연동해 줄 수 있습니다.
  - 예: **POST /api/v1/admin/receipts/:receiptId/tag** Body `{ "tag": "excellent_sample" | "suspected_fraud" }` 등

---

## 9. GEMS 자동 필터링 엔진 (상태 코드)

**상세:** [GEMS_자동_필터링_엔진.md](./GEMS_자동_필터링_엔진.md)

캠페인 정책(최소 금액, 기간, 지역)과 무결성(중복, OCR 신뢰도)을 기준으로 **자동 1차 분류** 후, 아래 상태를 제출/영수증 API에서 내려주시면 검수 큐·상태 조회에서 배지·필터로 반영합니다.

| 상태 코드 | 의미 | FE UI (배지 색상) |
|-----------|------|-------------------|
| `FIT` | 정상 접수 | Green |
| `UNFIT_AMOUNT` | 금액 미달 (숙박 6만/관광 5만 미만) | Orange |
| `OUT_OF_PERIOD` | 캠페인 기간 외 | Orange |
| `UNFIT_REGION` | 대상 지역 외 | Gray |
| `DUPLICATE` | 중복 제출 (MD5 등) | Red |
| `MANUAL_REVIEW` | 검토 대기 (신뢰도 낮음 등) | Yellow |

- Sidecar JSON에 `is_valid`, `reason`(또는 동등 필드)로 필터링 결과를 기록해 주시면, AI 학습·재학습 데이터셋 분리에도 활용 가능합니다.

---

## 10. 증거(영수증) 확인 – 데이터 교정 및 Sidecar (Data Curating)

증거 확인 단계는 단순 조회가 아니라 **데이터 교정(Data Curating)**의 핵심입니다. 담당자가 실패 사유(금액 미달, 지역 불일치 등)에 맞춰 값을 수정하고, **수정 사유**와 함께 **Sidecar JSON**에 기록되어야 AI 자산화·재학습 정답지(Ground Truth)로 활용할 수 있습니다.

### 10.1 교정 데이터 반영 API (권장)

- **PATCH /api/v1/admin/submissions/:receiptId/correction** (또는 PUT /receipts/:receiptId/sidecar 등)
- **Request Body 예시**:
  ```json
  {
    "total_amount": 65000,
    "address": "강원도 춘천시 ...",
    "correction_reason_code": "ocr_error",
    "correction_reason_detail": "빛 반사로 금액 인식 오류",
    "audit": { "actor_id": "admin@example.com", "at": "2026-03-05T12:00:00Z" }
  }
  ```
- **기대 동작**:
  - 해당 submission의 `total_amount` 등 DB 컬럼 업데이트
  - **Sidecar JSON**에 `[기존 값] → [수정 값] (수정자 ID, 시각)` 형태의 교정 이력 추가
  - `correction_reason_code`: `ocr_error` | `user_mistake` | `amount_sum_error` | `policy_exception` | `address_mismatch` | `other`
  - 교정 이력이 있으면 향후 AI 재학습 시 "사용자 입력이 맞고 OCR이 틀린 케이스" 등으로 분류해 학습 데이터로 활용

### 10.2 Sidecar JSON 구조 (참고)

보안 스토리지에 저장되는 Sidecar에는 다음이 포함되면 좋습니다.

- `receipt_id`: 제출(영수증) ID
- `ai_result`: OCR/AI 추출 결과 (amount, confidence 등)
- `human_correction`: 담당자 교정 정보
  - `final_amount`, `final_address` 등 확정값
  - `reason_code`: [GEMS 표준 수정·반려 사유 분류](./GEMS_표준_수정_반려_사유_분류.md)의 코드 (예: `ERR_OCR_AMOUNT`, `USER_AMOUNT_MISTAKE`)
  - `reason_desc`: 상세 설명 (또는 프리셋 detail)
  - `reviewed_by`, `at`
- `asset_tag`: 학습 데이터 분류용 (예: `RE_TRAINING_REQUIRED`, `USER_ERROR_LABEL`, `LOW_QUALITY_SAMPLE`, `FRAUD_CHECK`)

**표준 reason_code·asset_tag**는 [GEMS_표준_수정_반려_사유_분류.md](./GEMS_표준_수정_반려_사유_분류.md)를 따르면, AI 재학습 시 "왜 불일치했는가" 라벨로 활용할 수 있습니다.

---

## 요약 체크리스트

| 항목 | 우선순위 | 비고 |
|------|----------|------|
| 로그인 응답에 organization_id, org_name, org_type, campaignIds | 높음 | [로그인_API_조직정보_요청사항.md](./로그인_API_조직정보_요청사항.md) 참고 |
| GET /admin/submissions 에 from, to, campaignId, status 지원 | 높음 | 422 해결 후 대시보드·검수 필터 |
| 제출 항목에 project_type (STAY/TOUR) 포함 | 중간 | 대시보드·서브미션 업종 필터 |
| 제출 항목 확장: thumbnail_url, confidence, integrityCheck | 선택 | 서브미션 미리보기·신뢰도·무결성 |
| POST 일괄 반려 API (bulk-reject) | 중간 | 서브미션 일괄 반려 팝업 연동 |
| 일괄 반려 시 Audit Log (실행자, IP, 건수, 사유) | 권장 | 지자체 보안·감사 대비 |
| 대시보드 전용 집계 API (stats) | 선택 | 성능·일관성 |
| 반려 사유 집계 API | 선택 | “주요 반려 사유 Top 3” |
| 캠페인 start_date, end_date, budget 노출 | 높음 | 이미 있으면 유지 |
| 영수증 이미지 Signed URL 등 보안 처리 | 권장 | 보안 가이드라인 |
| 영수증 증거 열람 시 감사 로그(관리자 ID, 시각, IP) | 권장 | 상태 조회 화면 증거 보기 |
| 데이터 자산화 태깅 API (우수/부정수급 사례) | 선택 | 상태 조회 화면 태깅 버튼 |
| 자동 필터링 엔진 상태 코드 (UNFIT_AMOUNT, DUPLICATE 등) | 권장 | [GEMS_자동_필터링_엔진.md](./GEMS_자동_필터링_엔진.md) |
| 증거 확인 교정 API + Sidecar JSON 교정 이력 | 권장 | 데이터 교정·AI 자산화 (섹션 10) |
| 데이터 자산화 대시보드 KPI API | 선택 | 섹션 11 |
| 행정구역 통계 고도화 (최소금액·히트맵·Hash 검증 등) | 선택 | 섹션 12 |
| FE 콜백 판정 출처 구분 (statusSource: AUTO/MANUAL/SYSTEM) | 권장 | 섹션 13. 검수자 수동 FIT/UNFIT 시 FE 구분 표시 |

이 문서는 프론트엔드 구현 기준으로 작성되었으며, BE 스펙이 다를 경우 연동 시 조정이 필요할 수 있습니다.

---

## 11. 데이터 자산화 대시보드 KPI (선택)

대시보드 내 **"데이터 자산화 대시보드"** 섹션에서 아래 지표를 표시합니다. BE에서 집계 API를 제공하면 "—" placeholder를 실제 값으로 교체할 수 있습니다.

| KPI | 설명 | 예시 API/데이터 소스 |
|-----|------|----------------------|
| OCR 인식 정확도 (Raw) | 수정 없이 AI 판독값 그대로 승인된 비율 | 승인 건 중 human_correction 없는 비율 |
| 필드별 교정률 | 금액/일시/주소 필드별 담당자 수정 빈도 | Sidecar 교정 이력 집계 |
| 신뢰도-정답 일치도 | AI 신뢰도(%)와 실제 수정 여부 상관 | confidence vs correction 플래그 |
| 골든 데이터셋 누적 | 이미지+OCR+수정값 매핑 완료 건수 | receipt_items + human_correction 존재 |
| 재학습 필요 비중 | asset_tag=RE_TRAINING_REQUIRED 비율 | Sidecar asset_tag 집계 |
| 자동 판정 처리율 | 정책 미달 자동 반려 추천 수용 비율 | 자동 반려 추천 → 검수자 동의 건 |
| 검수 리드타임 | 제출 시각 ~ 최종 승인 시각 평균 | submissions.created_at, updated_at |
| 부정수급 탐지 적중률 | 의심 건 분류 중 실제 반려 비율 | anomaly 플래그 vs status=REJECTED |
| 교정률 추이 (선 그래프) | 주별 교정률 시계열 | 주차별 수정 건수 / 검수 건수 |
| 오류 유형 분석 (파이) | 반려/교정 사유별 건수 | reason_code 집계 |

- **GET /api/v1/admin/dashboard/assetization** (또는 기존 stats API 확장) 형태로 위 KPI를 한 번에 내려주면 대시보드에 연동 가능합니다.

---

## 12. 행정구역 통계 고도화 (선택)

**행정구역 통계** 페이지에서 캠페인 컨텍스트·AI 자산화·GEMS 운영 지침을 반영한 UI를 구성했습니다. 아래 API/데이터가 있으면 placeholder를 실제 값으로 교체할 수 있습니다.

| 기능 | FE 현재 | BE 연동 시 |
|------|---------|------------|
| 캠페인 기본 지역 | GET /api/v1/admin/context 의 adminSidoCode/adminSidoName으로 강원도 등 기본 선택 · 타 시도 Dimmed | 이미 연동 가능 (context API만 반환 시) |
| 최소 금액(6만/5만) 달성률 | "캠페인 기준 충족 건수 · API 연동 시 표시" | GET /api/v1/admin/stats/by-region 응답에 `minAmountAchieveCount`, `minAmountThreshold` 또는 별도 집계 API |
| 전국 시도 히트맵 | "접수 금액에 따른 색상 농도(API 연동 시 적용)" | 시도별 total_amount 또는 비율 전달 시 지도 path별 opacity/색상 스케일 적용 |
| 지역별 OCR 정확도 추이 | AI 자산화 카드 내 텍스트 안내 | Sidecar/교정 이력 기반 지역별 정확도 시계열 (선택) |
| 업종 분석 STAY vs CONSUME | 텍스트 안내 | Sidecar JSON 또는 project_type 집계 by region |
| 부정 수급 의심 핫스팟 | 텍스트 안내 | 동일 시간대·중복 제출 패턴 탐지 결과 by region (선택) |
| Hash(MD5) 검증 | 배지 + 툴팁 "API 연동 시" | 통계 집계 시 사용된 영수증 목록의 hash 검증 상태 플래그 |
| 영수증 샘플 바로보기 | /receipts?region=코드 링크 | 영수증 목록 API에 region(시군구코드) 필터 지원 시 해당 지역만 조회 |
| 보안 스토리지 경로 | `[지역코드]/[날짜]` 안내 문구 | 실제 저장 경로 규칙 문서화·일괄 추출 API (선택) |
| 개인정보 비식별화 | "외부 공유 시 AI 자동 마스킹" 안내 | 보고용 이미지셋 내보내기 시 마스킹 옵션 (선택) |

- **기존 GET /api/v1/admin/stats/by-region** 응답 확장 예: `items[]`에 `hashVerified?: boolean`, 시도 단위 응답 시 `amountBySido?: Record<sidoCode, number>` (히트맵용).

**BE 반영 요약 (행정구역 통계 고도화)**  
- **GET /api/v1/admin/context** (및 GET /api/v1/admin/me): 응답에 `adminSidoCode`, `adminSidoName` 추가 (소속 기관의 시도 코드/명. 타 시도 Dimmed 등 FE용).  
- **GET /api/v1/admin/stats/by-region**: 응답에 `minAmountAchieveCount`, `minAmountStay`, `minAmountTour`, `amountBySido`(level=SIDO일 때 시도별 금액), `items[].hashVerified`(현재 null) 추가.  
- **GET /api/v1/admin/submissions**: 쿼리 파라미터 `regionCode`(시군구 코드) 지원. 지정 시 해당 지역(첫 장 address/location 기준) 제출만 조회(영수증 샘플 바로보기 링크용).

---

## 13. FE 결과 콜백 – 판정 출처 구분 (statusSource)

OCR 인식률이 낮아 자동 FIT는 적고 UNFIT가 많으며, 실제 적격인데도 자동 UNFIT로 나와 사용자 클레임이 있는 상황입니다. 검수자 수동 판정 결과를 FE에 전달할 때 **로직(자동) FIT vs 검수자 수동 FIT vs UNFIT** 를 구분할 수 있도록 합니다.

### 13.1 콜백 payload 필드: statusSource

FE로 POST 되는 결과 콜백(OCR_RESULT_CALLBACK_URL) payload에 아래 필드가 포함됩니다.

| 필드 | 값 | 의미 |
|------|-----|------|
| **statusSource** | `AUTO` | **로직(OCR/정책) 자동 판정** 결과. 현재는 FIT일 때만 전송(오류율 감소 정책). |
| **statusSource** | `MANUAL` | **검수자 수동 판정** 결과. override 후 콜백 재전송(resend) 또는 callback/verify 시. FIT이면 "검수 승인", UNFIT이면 "검수 반려"로 FE 표시 가능. |
| **statusSource** | `SYSTEM` | **시스템 처리** (예: verifying_timeout 크론 등). |

- 기존 필드 `status` / `overall_status`: `FIT` | `UNFIT` | `ERROR` 등 그대로 사용.
- FE 권장: `statusSource === "MANUAL"` 이면 "검수 결과를 반영했습니다" 등으로 구분 표시; `statusSource === "AUTO"` 이면 "자동 적격" 등으로 표시.

### 13.2 검수자 수동 시 콜백 전송

- **POST /api/v1/admin/submissions/:receiptId/override**: 검수자가 상태를 FIT 또는 UNFIT로 변경할 때, 요청 body의 **resend_callback: true** 이면 변경된 최종 상태로 FE에 콜백을 한 번 전송합니다. 이때 **statusSource = "MANUAL"** 이므로 FE는 검수 승인/반려로 구분할 수 있습니다.
- **POST /api/v1/admin/submissions/:receiptId/callback/resend**: 관리자가 "콜백 재전송"을 누를 때도 **statusSource = "MANUAL"** 로 전송됩니다.

### 13.3 정책 요약

| 구분 | FIT만 전송 여부 | statusSource |
|------|------------------|--------------|
| 자동 분석 완료 (purpose=auto) | FIT일 때만 전송 (UNFIT 등 스킵) | `AUTO` |
| 검수자 override 후 재전송 (resend_callback=true) | FIT/UNFIT 모두 전송 | `MANUAL` |
| 관리자 콜백 재전송/검증 (resend, verify) | FIT/UNFIT 모두 전송 | `MANUAL` |
| verifying_timeout 크론 | 전송 (action 적용 건) | `SYSTEM` |
